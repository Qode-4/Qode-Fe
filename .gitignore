# Dependencies
node_modules/

# Build outputs
dist/
out/
release/

# TypeScript
*.tsbuildinfo

# Caches
.eslintcache
.vite/

# Logs
*.log*
artifacts/

# Env (keep templates if you add them)
.env
.env.*
!.env.example
!.env.sample

# Yarn (Berry) local artifacts
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# OS / IDE
.DS_Store
Thumbs.db
Desktop.ini
.idea/
*.iml

# Local-only docs / agent config
docs-private/
.codex/
.claude/
.superpowers/

# 로컬에서 만든 웹 배포 산출물.
# VITE_API_BASE_URL 이 빌드 시점에 박히므로 로컬 빌드본을 올리면 운영이
# localhost 를 때린다(CSP 까지 localhost 만 허용이라 API 가 전부 차단된다).
# 배포는 CI 로만 한다 — .github/workflows/web-deploy-ec2.yml 이 secrets 로
# .env 를 새로 만들어 빌드한다.
web-dist.tar.gz
*.tar.gz

# Electron-builder artifacts (in case output is configured outside dist/)
*.dmg
*.exe
*.AppImage
*.deb
*.rpm
*.snap
*.blockmap
*.asar

#ai
/.omx
/.omc
storybook-static
