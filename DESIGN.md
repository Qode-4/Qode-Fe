# Qode Design

## Source of truth — 문서 상태와 근거

- 상태: Needs refresh. 사용자 요청으로 전체 UI에 1차 적용했으며, 실제 사용 피드백으로 조정한다.
- 갱신일: 2026-09-15.
- 목적: Qode의 기존 기능을 일관된 디자인으로 표현하기 위한 기준서다.
- 적용 화면: 프로젝트 작업 공간을 우선으로 하며 로그인·가입·초대·프로젝트 생성·저장소·설정에도 공통 시각 규칙을 적용한다.
- 공통 컬러·타이포·버튼·입력창·카드에 1차 적용했다. 실제 스타일 구현의 기준 파일은 `src/renderer/src/assets/main.css`다. 화면별 추가 조정과 시각 검증은 남아 있다.
- 근거: 사용자가 제공한 Replit 스타일 레퍼런스와 대화에서 확정한 요구사항, `README.md`, `AGENTS.md`, 현재 페이지와 컴포넌트 코드.
- 참고 자료: `/Users/yeji/Downloads/AI-UIUX-설계-실무가이드.pdf`. 정보 위계, 실제 콘텐츠 중심 설계, 상태별 화면, 접근성, 평가 체크리스트 원칙을 반영한다. 문서 내 외부 제품·도구 관련 주장은 별도로 검증된 사실로 취급하지 않는다.
- 확인한 구현: `components/layout/AppShell.tsx`, `components/ui/DrawerHeader.tsx`, `components/ui/Button.tsx`, `components/ui/ChatComposer.tsx`, `pages/ProjectDetailPage.tsx`, `api/capabilities.ts` 등. 경로는 별도 표시가 없으면 `src/renderer/src/` 기준이다.
- 이전 생성 시안은 탐색 자료이며 승인된 구현 명세가 아니다. 실제 구현과 충돌하는 시안 요소를 기능 요구사항으로 삼지 않는다.

## Brand — 브랜드

**차분하고 신뢰감 있는 개발 도구, 따뜻한 캔버스 위의 오렌지 액션.**

- 크림색 바탕, 읽기 쉬운 콘텐츠, 절제된 오렌지로 따뜻하고 집중하기 좋은 작업 공간을 만든다.
- 브랜드 포인트는 사용자 확정값 `#ff6900`이다. 레퍼런스의 `#ff3c00`으로 대체하지 않는다.
- 신뢰는 실제 동기화 상태, 답변의 참조 소스, 명확한 오류와 복구 안내로 표현한다.
- 과장된 AI 성능 문구, 임의의 통계, 장식용 배지, 미구현 기능을 암시하는 컨트롤은 사용하지 않는다.

## Product goals — 제품 목표

- 프로젝트 코드를 질문하고 답변의 근거를 확인하는 흐름을 쉽게 만든다.
- 프로젝트와 대화를 빠르게 구분하고 이어서 작업할 수 있게 한다.
- 동기화 상태와 실행 가능 여부를 쉽게 파악하게 한다.
- 성공 신호: 사용자가 현재 프로젝트를 식별하고, 질문을 전송하고, 답변 근거를 찾고, 오류에서 복구할 수 있다.
- 비목표: 홍보용 랜딩 페이지, 새로운 IDE, 대시보드, 미구현 기능의 선행 디자인.

## Personas and jobs — 사용자와 작업

- 주 사용자 가정: 팀 프로젝트의 코드를 이해하고 협업하는 개발자.
- 보조 사용자 가정: 구현 맥락을 파악하려는 기획자 등 협업자. 사용자 조사로 검증된 페르소나는 아니다.
- 핵심 작업: 프로젝트 선택 → 코드 질문 → 답변 읽기 → 참조 소스 확인 → 후속 질문.
- 관련 작업: 프로젝트 연결·동기화, 개인 대화 관리, 팀 대화, 저장소 관리.
- 사용 맥락: 한국어 중심의 데스크톱 작업, 긴 답변과 코드 읽기, Electron과 웹 양쪽 이용.

## Information architecture — 정보 구조

### 현재 구현

- 자체 해시 라우터로 인증 화면, 프로젝트 화면, 저장소 화면 등을 연결한다.
- 사이드바에는 프로젝트 선택·설정, 섹션, 내 채팅, 팀 채팅, 사용자 설정 진입점이 있다.
- AI 대화에는 사용자 메시지, 마크다운 답변, 코드 블록, 참조 소스 목록과 메시지 작업이 있다.
- 참조 소스는 답변 아래에서 펼치거나 접는 파일 경로·줄 번호 목록이다. 별도의 코드 미리보기 패널이 아니다.
- 확인 시점의 `API_CAPABILITIES`는 `teamChatWritable: true`, `messageShareEnabled: false`다. 팀 채팅 작성은 가능하며 답변의 팀 공유는 비활성이다. 답변의 팀 채팅 생성 액션도 현재 비활성이다.

### 확정된 변경 요구사항

- 사이드바의 **섹션 영역을 임시로 숨긴다**. 단순히 접힌 상태로 남기는 것이 아니며, 데이터와 기능 삭제를 의미하지 않는다.
- **동기화 버튼을 설정 메뉴 밖에 노출한다**. 프로젝트 설정의 다른 기능은 유지한다.
- 동기화는 기존 권한·프로젝트 선택·진행 상태·요청 중 조건을 보존한다. 메뉴 밖으로 옮긴다고 권한을 확대하지 않는다.
- **동기화 버튼 오른쪽에 `마지막 동기화: N분 전` 텍스트를 인라인으로 붙인다**. 시각은 실제 `lastSyncedAt` 값을 상대 시간으로 표시한다. 값이 없거나 `queued`·`syncing`·`failed` 상태이면 그 상태 문구로 대체한다.
- **`내 채팅` 섹션 헤더 오른쪽에 `+` 새 채팅 만들기 버튼을 둔다**. 접기·펼치기 화살표는 사용하지 않으며, 섹션 하단의 별도 `+ 추가` 링크도 제거한다. `팀 채팅` 헤더는 `teamChatWritable`가 true인 동안만 같은 위치에 `+` 버튼을 두고, false이면 버튼을 숨긴다.
- **사이드바 하단에 사용자 프로필 행을 고정한다**: 오렌지 원형 아바타(이니셜), 이름, `⋮` 더보기 버튼만 표시한다. 이메일과 로그아웃 등 상세 정보는 `⋮` 클릭 시 열리는 기존 설정 메뉴 안에 둔다.
- 지원하지 않는 검색, 첨부, 음성 입력, AI 도구 메뉴, 팀 위키, 소스 미리보기 패널을 추가하지 않는다.
- 기존 저장소와 설정 기능의 진입 경로를 보존한다. 생성 이미지에서 빠졌다는 이유로 제거하지 않는다.

## Design principles — 설계 원칙

1. 질문과 답변이 중심이다. 상태 안내와 보조 작업이 본문을 밀어내지 않게 한다.
2. 시각적 강조는 중요도에 비례한다. 오렌지는 주요 행동과 현재 위치에 집중한다.
3. 관련 정보를 가까이 배치한다. 답변 근거는 해당 답변 아래, 동기화 정보는 해당 프로젝트 맥락에 둔다.
4. 같은 상태를 여러 위치에 반복하지 않는다. 진행률·완료 상태·실패 안내의 역할을 구분한다.
5. 기존 컴포넌트와 기능을 재사용한다. 외형 개선을 새 기능의 근거로 삼지 않는다.
6. 정보 밀도와 읽기 편함을 함께 지킨다. 랜딩 페이지의 대형 여백과 타이포를 작업 화면에 그대로 적용하지 않는다.

## Visual language — 시각 언어

### 컬러와 토큰

아래는 **목표 매핑**이다. 기존 토큰 이름을 유지하며, 새 토큰은 실제 사용처가 생길 때만 추가한다. 원시 팔레트 전체를 중복 등록하지 않는다.

| 시맨틱 토큰                          | 목표값       | 용도                        |
| ------------------------------------ | ------------ | --------------------------- |
| `app-bg`                             | `#faf6f1`    | 앱 캔버스                   |
| `sidebar`                            | `#fbfaf8`    | 고정 사이드바               |
| `surface`                            | `#ffffff`    | 콘텐츠·입력창·모달          |
| `surface-muted`                      | `#faf6f1`    | 보조 콘텐츠 면              |
| `line`                               | `#dfddd8`    | 장식적 구분선·카드 경계     |
| `line-soft`                          | `#efefef`    | 약한 내부 구분선            |
| `text-base`                          | `#0e0e0f`    | 제목·핵심 본문              |
| `text-subtle`                        | `#52545a`    | 보조 본문                   |
| `text-soft`                          | `#61646b`    | 설명·시간·플레이스홀더      |
| `primary`                            | `#ff6900`    | 주요 액션 채움              |
| `primary-strong`                     | `#e05e00`    | 주요 액션 hover·pressed     |
| `primary-foreground`                 | `#0e0e0f`    | 오렌지 버튼 글자·아이콘     |
| `accent`                             | `#ff6900`    | 브랜드 강조·선택 표시       |
| `primary-soft`                       | `#fff0e5`    | 활성 내비게이션의 옅은 배경 |
| `accent-strong`                      | `#b54700`    | 밝은 면의 링크·강조 텍스트  |
| `danger`, `danger-bg`, `danger-line` | 기존 값 유지 | 오류·삭제 경고              |

- 선택 행은 `primary-soft` 면, `accent` 세로 좌측 인디케이터, 진한 글자를 조합한다.
- `#dfddd8`은 장식용 경계다. 입력창 등 식별에 필수적인 경계는 `#61646b`, 포커스는 `#0e0e0f`를 사용한다.
- 레퍼런스의 잘못된 색상값 `#312e2`는 `#312e2e`로 해석한다. 별도 토큰은 필요할 때만 추가한다.
- 블루·피치·코랄 팔레트와 브랜드 그라데이션은 현재 핵심 작업 화면에 도입하지 않는다.
- `primary-foreground` 변경 시 현재 이를 재사용하는 danger 버튼도 확인한다. 오류 버튼의 전경은 별도로 적정 대비를 유지해야 한다.

### 타이포그래피

- 기존 Pretendard 및 시스템 폰트 대체 체계를 유지한다. ABC Diatype 등 새 폰트를 설치하지 않는다.
- 아래 크기는 기본 배율 기준이며 기존 `--font-size-base` 연동 토큰으로 표현한다. 글자 확대 설정을 우회하는 고정 px를 새로 사용하지 않는다.

| 역할           | 기본 크기 | 행간 | 굵기    |
| -------------- | --------- | ---- | ------- |
| 설명·시간      | 12px      | 1.5  | 400     |
| 내비·컨트롤    | 14px      | 1.5  | 400–500 |
| 답변 본문      | 16px      | 1.6  | 400     |
| 작업 화면 제목 | 20px      | 1.3  | 500–600 |
| 빈 화면 제목   | 32–42px   | 1.2  | 400–500 |

- 한국어 본문 자간은 기본값을 사용한다. 제목만 최대 `-0.02em`까지 허용한다.
- 레퍼런스의 `-0.03px`와 `-0.03em`을 같은 값으로 취급하지 않는다. 자간 단위는 `em`으로 명시한다.
- 코드에는 기존 고정폭 폰트를 사용하고, 긴 줄은 코드 영역 내부에서 가로 스크롤한다.

### 간격·형태·표면

- 기본 간격 단위 4px. 주로 4·8·12·16·20·24·32px를 사용한다.
- 내비·버튼·입력창은 6px, 코드·참조 소스 패널은 8px, 메시지 말풍선은 12px 라운딩을 기준으로 한다. 모달은 기존 공통 컴포넌트의 라운딩을 따른다.
- 버튼은 알약형으로 만들지 않으며, 6px 라운딩의 명확한 사각 컨트롤을 사용한다. 아이콘 버튼도 같은 기준을 따른다.
- 콘텐츠 간격은 12–24px를 기본으로 하며 답변 길이와 그룹 관계에 맞춘다.
- 40–60px 카드 모서리, 80px 섹션 간격, 64px 디스플레이 글자는 핵심 작업 화면에 적용하지 않는다.
- `#faf6f1` 캔버스 위에 `#fbfaf8` 사이드바와 흰색 메인 작업 영역을 맞닿게 배치한다. 메인 작업 영역을 떠 있는 큰 카드로 감싸지 않으며, 사이드바와의 1px 구분선으로 구조를 만든다. 무거운 그림자와 장식용 카드 중첩을 피한다.
- 아이콘은 기존 SVGR 레지스트리와 `Icon`을 사용한다. 새 로고·장식 일러스트를 생성하지 않는다.
- 애니메이션은 상태 전달에 한정하고 지속적인 장식 움직임을 사용하지 않는다.

## Components — 컴포넌트 기준

- `AppShell`, `DrawerHeader`, `ProjectSwitcher`: 240px 사이드바의 상단에 Qode 워드마크 행, 그 아래 테두리 프로젝트 선택기와 설정 버튼 행을 둔다. 섹션은 숨기고, 선택기 아래에 동기화 버튼과 `마지막 동기화: N분 전` 인라인 텍스트를 나란히 둔다. `내 채팅`·`팀 채팅` 섹션 헤더는 접기 화살표 없이 라벨과 오른쪽 `+` 새 채팅 버튼(권한·capability에 따라 표시)만 둔다. 사이드바 하단에는 사용자 프로필 행(오렌지 원형 아바타·이름·`⋮`)을 고정하고 이메일·로그아웃은 `⋮` 메뉴 안에서만 노출한다.
- `Button`, `IconButton`, `TextField`, `OverlayModal`: 공통 토큰·포커스·disabled 규칙 재사용. 주요 버튼의 기존 그라데이션은 목표 스타일에서 단색으로 전환한다.
- `ChatComposer`: 기존 입력·전송·자동 높이·길이 제한을 유지한다. 하단 전체 폭 footer에서 흰색 textarea와 별도의 48px 정사각형 오렌지 전송 버튼을 나란히 둔다. 첨부 버튼을 추가하지 않는다.
- AI 답변 헤더는 오렌지 원형 `Q` 마크와 `Qode AI` 레이블을 사용한다. 완료된 답변과 스트리밍 중 답변 모두에 같은 헤더를 붙여 시각 정체성을 통일한다. 이는 기존 AI 응답의 식별 표현이며 새 에이전트 기능을 뜻하지 않는다.
- `MarkdownAnswer`, `CodeBlock`, 참조 소스 목록: 읽기 흐름을 유지한다. 코드 블록은 어두운 표면과 밝은 텍스트를 사용하고, 참조 소스 패널은 `참조한 소스 N개` 헤더에 아래 방향 셰브런을 두고 파일 경로는 좌측, `시작-끝` 줄 번호는 우측에 배치한다. 파일 경로와 줄 번호는 실제 응답값만 표시한다.
- 답변 하단 메시지 액션은 `복사`, `팀 공유`, `팀 채팅 생성`의 3개 버튼으로 고정한다. 각 버튼은 실제 `API_CAPABILITIES`와 권한에 따라 활성·비활성 상태를 갖고, 비활성 시에는 이유를 툴팁으로 안내한다.
- `InlineAlert`, `Toast`: 오류 원인과 복구 행동을 설명한다. 오렌지를 성공·실패 공통 상태색으로 사용하지 않는다.
- `ProjectTabs`, 저장소 테이블, 생성·설정 모달: 기존 사용처를 유지하고 공통 규칙을 확장한다.
- 기능 활성 여부는 실제 capability와 권한 로직을 따른다. 문서의 시점별 기록보다 최신 구현을 확인한다.

## Accessibility — 접근성

- 목표: WCAG 2.1 AA. 이 문서는 현재 앱의 준수를 인증하지 않는다.
- 일반 텍스트 대비 4.5:1 이상, 큰 글자와 필수 비텍스트 UI 대비 3:1 이상을 확인한다.
- sRGB 상대 휘도로 계산한 조합: `#ff6900`/흰색 2.89:1, `#ff6900`/`#0e0e0f` 6.68:1, `#b54700`/흰색 5.43:1, `#b54700`/크림 5.05:1.
- 따라서 오렌지 버튼은 진한 글자·아이콘을 사용하고, 밝은 면의 작은 오렌지 링크는 `accent-strong`을 사용한다.
- `#76716f`/크림은 약 4.47:1이므로 일반 보조 텍스트에 채택하지 않는다.
- 키보드 포커스는 진한 2px 외곽선과 2px 간격을 기본으로 한다. 색상 외에도 선택·오류를 텍스트나 형태로 구분한다.
- 버튼·링크·제목·랜드마크를 시맨틱하게 작성하고, 본문 건너뛰기 경로와 자연스러운 Tab 순서를 검토한다.
- 아이콘 버튼에는 접근 가능한 이름을 제공한다. 입력창에는 placeholder와 별도로 레이블을 연결한다.
- 모달의 초점 진입·가두기·복귀, Escape 동작과 메뉴 키보드 조작을 확인한다.
- 스트리밍 알림이 답변 전체를 매 토큰마다 반복해서 읽지 않게 한다. 진행 상태와 오류 알림의 빈도를 제한한다.
- `prefers-reduced-motion`과 기존 글자 확대 설정을 존중한다.

## Responsive behavior — 화면 크기 대응

- 우선 대상은 Electron과 데스크톱 웹이다. 모바일 전용 재설계는 이번 범위에 포함하지 않는다.
- 사이드바는 240px, 메인 작업 영역은 흰색의 flush 면으로 둔다. 대화 본문 최대 폭은 48rem으로 제한한다. 작은 창의 내비 처리 방식은 후속 검증 항목이다.
- 입력창과 주요 동작이 창 아래로 잘리지 않도록 본문 스크롤과 고정 영역을 구분한다.
- 긴 제목은 생략 표시와 전체 이름 확인 수단을 제공하고, 긴 파일 경로와 코드가 앱 전체 너비를 늘리지 않게 한다.
- hover로 나타나는 작업은 키보드 focus로도 접근 가능해야 한다. 터치 환경에서도 hover만을 유일한 진입 조건으로 삼지 않는다.

## Interaction states — 상태별 표현

| 상태                    | 표현과 행동                                               |
| ----------------------- | --------------------------------------------------------- |
| 초기 로딩               | 해당 영역의 로딩 안내, 불필요한 전체 화면 교체 방지       |
| 프로젝트·대화 없음      | 현재 맥락과 실제 가능한 다음 행동 안내                    |
| 답변 생성 중            | 진행 문구, 부분 답변, 기존 전송 제한 유지                 |
| 동기화 대기·진행        | 동기화 버튼 비활성화와 진행 문구, 실제 진행률만 사용      |
| 동기화 성공             | 서버가 제공한 마지막 동기화 시점 표시                     |
| 동기화 실패             | 원인과 기존 재시도 동작 제공                              |
| 조회·전송 실패          | 기존 내용을 보존하고 해당 요청의 복구 행동 안내           |
| 권한 부족·미지원        | 실제 권한과 capability에 따른 숨김 또는 비활성, 이유 안내 |
| 느린 네트워크·연결 끊김 | 실제 연결 상태와 재연결 안내, 입력 내용 보존              |

## Content voice — 문구

- 한국어를 기본으로 하고 짧고 구체적인 문장을 사용한다.
- 버튼은 실행 결과를 말한다: `동기화`, `다시 시도`, `새 채팅`, `이름 바꾸기` 등.
- 같은 동작은 흐름 전체에서 같은 이름으로 부른다.
- 오류는 무엇이 실패했는지와 다음에 할 수 있는 행동을 설명한다.
- AI 답변의 정확성을 보장하는 문구나 존재하지 않는 근거·수치를 만들지 않는다.
- 시안의 예시 콘텐츠와 줄 번호는 목데이터로 표시하며 실제 저장소 분석 결과로 주장하지 않는다.

## Implementation constraints — 구현 제약과 평가

- React·TypeScript·Tailwind v4 및 기존 컴포넌트 계층을 유지한다. 새로운 디자인 시스템 패키지나 폰트 의존성을 추가하지 않는다.
- 렌더러는 Electron API에 의존하지 않는다. 해시 라우팅, API·SSE·Socket 동작과 인증 정책을 보존한다.
- API 생성 파일을 직접 수정하지 않는다. 이번 문서 작성으로 API·타입·의존성을 변경하지 않는다.
- 사용자 확인 순서: 기준서 → 실제 기능에 근거한 시안 → 사용자 검토 → UI 구현. 2026-09-15 사용자 요청으로 전체 디자인을 우선 적용하고 사용하며 조정하는 방식으로 전환했다.
- 스타일 구현 시 변경된 Storybook과 실제 화면을 확인하고 lint·typecheck를 실행한다. 문서만 작성하는 단계에서는 문서·토큰·기능 사실·대비 계산을 검증한다.

### 시안·구현 검토 체크리스트

- [ ] 주요 과제가 시각적으로 가장 분명하고 동일 정보가 중복되지 않는다.
- [ ] 섹션이 숨겨져 있고, 프로젝트 선택기 아래 동기화 버튼과 인라인 `마지막 동기화: N분 전` 텍스트가 함께 보인다.
- [ ] `내 채팅`·`팀 채팅` 섹션 헤더는 접기 화살표 없이 오른쪽 `+` 새 채팅 버튼만 노출하고, 권한·capability 규칙을 따른다.
- [ ] 사이드바 하단 사용자 프로필 행은 오렌지 원형 아바타·이름·`⋮` 만 표시하고 이메일·로그아웃은 `⋮` 메뉴 안에서만 노출한다.
- [ ] 완료된 AI 답변과 스트리밍 중 답변 모두 오렌지 원형 `Q` + `Qode AI` 헤더를 사용한다.
- [ ] 참조 소스 패널의 헤더·파일 경로·줄 번호 정렬과 아래 방향 셰브런이 시안과 일치한다.
- [ ] 답변 하단에 `복사`·`팀 공유`·`팀 채팅 생성` 3개 버튼이 순서대로 있고, capability에 맞춘 비활성 상태와 툴팁이 유지된다.
- [ ] 기존 설정·저장소·대화 기능의 진입 경로와 권한을 보존한다.
- [ ] 미구현 기능이나 가공한 운영 지표가 추가되지 않았다.
- [ ] 빈 화면·로딩·생성 중·실패·비활성 상태가 정의되어 있다.
- [ ] 긴 한국어 제목, 긴 답변, 긴 파일 경로와 코드에서 레이아웃을 확인했다.
- [ ] 글자 확대와 키보드 조작에서 입력창·메뉴·동기화에 접근 가능하다.
- [ ] 기본·hover·focus 상태의 대비와 토큰 참조를 확인했다.
- [ ] 실제 화면에서 C 시안의 flush 메인 영역, 240px 사이드바, 선택 내비게이션, composer를 시각 검증했다.
- [ ] 같은 요소의 재구현 없이 기존 컴포넌트를 재사용한다.

## Open questions — 다음 시안에서 확정할 사항

- [x] 동기화 위치: 프로젝트 선택 아래. OWNER에게 노출하며 기존 진행 상태와 요청 중 비활성화를 유지하고 실제 마지막 동기화 시각을 함께 표시한다.
- [x] C 시안 기준: 대화 최대 폭 48rem, 사이드바 240px, 선택 행은 옅은 오렌지 면과 오렌지 좌측 인디케이터, 내비·버튼·입력창 6px / 코드·소스 8px / 말풍선 12px 라운딩.
- [ ] 좁은 창의 내비 전환 방식과 최소 지원 크기. 담당: 사용자와 구현 담당자. 새 기능을 암묵적으로 추가하지 않고 후속 시안 범위에서 결정한다.
- [ ] 실제 주 사용자와 이용 환경 검증. 담당: 제품 담당자. 현재 개발자 중심 가정을 확인한다.

실제 화면의 시각 검증은 아직 완료되지 않았다. 남은 질문은 좁은 창과 실제 사용자 환경 검증이며, 확정된 C 시안 규칙은 구현 기준으로 사용한다.
